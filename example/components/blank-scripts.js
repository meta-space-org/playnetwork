// list of scripts that should work on client-side without their back-end counterpart

pc.createScript('room');
pc.createScript('camera');
pc.createScript('username');
pc.createScript('leaveButton');
pc.createScript('uiRoomId');
